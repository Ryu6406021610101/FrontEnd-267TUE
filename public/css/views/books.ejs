<!--books.ejs -->
<!DOCTYPE html>
<html>
    <head>
        <title>Books</title>
    </head>
    <link rel="stylesheet" href="../css/style.css">
    <body>
        <h1>Books</h1>
        <table>
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>aCTION</th>
                
            </tr>
            <% for(var i=0; i <books.length; i++) { %>
                <tr>
                    <td><%= books[i].title %></td>
                    <td><%= books[i].author %></td>
                    <td>
                        <a href="/bokok<%= books[i].id %>">View</a> 
                        <a href="/update/<%= books[i].id %>">Edit</a> 
                        <a href="/Delete/<%= books[i].id %>/delete">Delete</a>
                    </td>
                </tr>
            <% } %>
        </table>
        <a href="/create">Create new Book</a>
    </body>
</html>